"use client";

import styled from "styled-components";

export const StyledPasswordInfo = styled.p`
  margin-bottom: 2.4rem;
  font-family: var(--instrument-sans-regular);
  font-size: 1.2rem;
  line-height: 150%;
`;
